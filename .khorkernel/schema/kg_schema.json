{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Khora Kernel Knowledge Graph Schema v1.0.2",
  "description": "Defines the structure for concepts.json and rules.json generated by populate_kg.py.",
  "type": "object",
  "additionalProperties": {
    "$ref": "#/definitions/knowledgeItem"
  },
  "definitions": {
    "knowledgeItem": {
      "type": "object",
      "properties": {
        "desc": {
          "description": "The description of the concept or rule extracted from Markdown.",
          "type": "string"
        },
        "source": {
          "description": "The relative path to the Markdown file where the item was defined.",
          "type": "string",
          "format": "uri-relative"
        }
      },
      "required": [
        "desc",
        "source"
      ],
      "additionalProperties": false
    }
  }
}